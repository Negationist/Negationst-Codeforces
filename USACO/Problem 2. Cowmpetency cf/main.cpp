#include <bits/stdc++.h>
#define int long long
#define pb push_back
#define ff first
#define ss second
#define pii pair<int,int>
#define vi vector<int>
#define vii vector<pair<int,int>>

using namespace std;

void solve(){
    int n,q,c;
    cin >> n >> q >> c;
    for(int i=0;i<q;i++){

    }
    vector<vi> dp(q,vi(c));
    vector<vi> pref(q,vi(c));
    for(int i=0;i<q;i++){

    }
}

signed main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    solve();
    return 0;
}
