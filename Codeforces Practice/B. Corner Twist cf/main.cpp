#include <bits/stdc++.h>

#define FOR(i,n) for(int i=0;i<n;i++)
#define ll long long

using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--){
        int n,m;
        cin >> n >> m;
        string a[n],b[n];

    }
    return 0;
}
