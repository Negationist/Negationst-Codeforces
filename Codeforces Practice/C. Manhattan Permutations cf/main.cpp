#include <bits/stdc++.h>

#define FOR(i,n) for(int i=0;i<n;i++)
#define int long long

using namespace std;

void solve(){
    int n,k;
    cin >> n >> k;
    if(k%2==1){
        cout << "NO\n";
        return;
    }
    int mx = 0;
    for(int i=1;i<=n;i++){
        mx += abs(n-)
    }
}

signed main()
{
    int t;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}
